version: "3.9"
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5001:5001"
    volumes:
      - ./ml:/app/ml
      - ./data:/app/data
    command: python backend/app.py
